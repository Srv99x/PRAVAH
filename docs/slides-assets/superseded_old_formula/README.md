# SUPERSEDED - do not use these screenshots

These six images were captured before the priority decision matrix
(merged in 4818161). They show the OLD rule, `risk = trigger x susceptibility
multiplier`, a 0.00-1.00 "risk" scale, a 50% review threshold, and the old
"Early Warnings" wording. That rule could never flag a Low or Moderate cell;
see `docs/priority_matrix.md`.

Current screenshots are `docs/slides-assets/ss_map.png`, `ss_explain.png` and
`ss_ranked.png`, regenerated with `venv\Scripts\python.exe scripts/capture_screenshots.py`.

Kept only for history. The IoT-telemetry and sidebar-disclosure views
(`demo_05`, `demo_06`) have no current replacement yet.
