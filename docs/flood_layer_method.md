# Flood layer: locally derived HAND from SRTM 30 m

Generated by `app/flood_terrain.py`. **This is a terrain susceptibility proxy, not an inundation model.**

## What was computed

Source: the SRTM DEM already used by the terrain pipeline (`data/raw/kamrup_metro_dem.tif`, 1 arc-second, integer metres) and the 904-cell 1 km grid. Nothing is taken from Google Earth Engine except the cross-check in section 4. Steps, all with `pysheds` 0.5:

1. Condition the DEM: fill pits, fill depressions, resolve flats (277,789 of 1,937,520 pixels raised; median raise 2 m, 26,909 pixels raised by more than 5 m, largest 70 m; 17 negative-elevation pixels in the raw DEM, all removed by filling).
2. D8 flow direction and area-weighted flow accumulation (pixel area varies with latitude).
3. Channels = pixels with accumulated upstream area above **1 km2**. A pixel is 856-858 m2 across the tile, so the threshold is **about 1,166-1,168 pixels** (area-weighted, so exact per row). 36,723 channel pixels result.
4. HAND: for every pixel, its height on the depression-filled DEM minus the height of the channel pixel it drains into along the D8 path (path walk written for this project; pysheds' `compute_hand` is the cross-check below). 1,873,457 pixels have a value; 64,063 drain out of the tile (or into an unresolved flat or sink) before reaching a channel and are left empty.
5. Per grid cell: `hand_min`, `hand_mean` (over pixels with a valid HAND), `upa_max` (km2), `chan_dist_m` (cell centroid to the nearest channel pixel centre, metres, local equirectangular), `n_valid_hand` (pixels with a valid HAND).

Checks on step 4. (a) 3000 random pixels were walked one step at a time along the D8 directions: 0 disagreed with the computed HAND. (b) pysheds' own `compute_hand` was run as a cross-check: on the 1,865,834 pixels where both give a value there are 0 differences. pysheds leaves 7,623 further pixels empty, and every one of them has a flow path that passes through the outermost ring of the tile (pysheds treats the tile edge as a boundary); HAND here is computed by walking the D8 path directly, so those pixels keep a value. Nowhere does pysheds give a value where this computation gives none. 7,623 pixels with a value have a path touching the edge ring; they lie next to the tile boundary and are the least reliable.

## 1. Cells with no valid values, and why

- **93 cells have no DEM pixels at all**, so every feature is empty. They are exactly the 93 cells in `data/processed/missing_terrain_cells.json`: the DEM tile ends at 26.200 N and 92.150 E, while the grid extends to 26.255 N and 92.178 E.
- 9 further cells have DEM pixels but none with a valid HAND (all their pixels drain off the tile edge or into an unresolved sink before reaching a channel).
- 98 cells have valid values but fewer than half a full cell's pixels (under 594 of about 1188), because the DEM tile clips them. Their values describe only the covered part; use `n_valid_hand` to filter.
- `chan_dist_m` is empty for the 119 cells whose centroid lies outside the DEM tile.

## 2. Sanity check

Seven checks. Six were written before HAND was computed; the seventh replaced a failed check after the first run (section 3).

Ranking uses `hand_mean`, ties broken by `hand_min`, on the 704 cells with at least half a cell of valid pixels. Many cells contain a channel pixel and so have `hand_min = 0`, which is why `hand_mean` ranks them.

| Check | Result | Required |
|---|---|---|
| 10 lowest-HAND cells in the lowest elevation quartile (of 10) | 10 | >= 8 |
| 10 highest-HAND cells in the top elevation quartile (of 10) | 10 | >= 8 |
| 10 highest-HAND cells steeper than the district median slope (of 10) | 10 | >= 10 |
| Spearman, hand_mean vs elevation_mean | 0.898 | >= 0.5 |
| Spearman, hand_mean vs slope_mean | 0.946 | >= 0.3 |
| Median hand_mean of the 71 steepest-decile cells vs district median (replaces a failed check, see section 3) | 115.202 | > 21.881 |
| Share of the 28 cells within 3 km of 26.14 N 91.77 E with below-median HAND | 0.714 | >= 0.6 |

### 10 lowest-HAND cells

| grid_id | centroid_lat | centroid_lon | hand_min | hand_mean | elevation_mean | slope_mean |
|---|---|---|---|---|---|---|
| KM_R020_C009 | 26.183 | 91.725 | 0.000 | 0.000 | 42.000 | 0.009 |
| KM_R019_C002 | 26.173 | 91.655 | 0.000 | 0.002 | 42.002 | 0.031 |
| KM_R013_C002 | 26.119 | 91.656 | 0.000 | 0.030 | 42.964 | 0.778 |
| KM_R014_C001 | 26.128 | 91.646 | 0.000 | 0.041 | 44.339 | 1.463 |
| KM_R013_C003 | 26.119 | 91.666 | 0.000 | 0.065 | 44.734 | 1.620 |
| KM_R021_C010 | 26.192 | 91.735 | 0.000 | 0.075 | 42.054 | 0.103 |
| KM_R013_C004 | 26.119 | 91.676 | 0.000 | 0.087 | 46.159 | 1.918 |
| KM_R017_C048 | 26.159 | 92.115 | 0.000 | 0.114 | 50.916 | 1.698 |
| KM_R014_C003 | 26.128 | 91.666 | 0.000 | 0.114 | 46.155 | 2.182 |
| KM_R012_C005 | 26.112 | 91.686 | 0.000 | 0.142 | 46.633 | 1.850 |

### 10 highest-HAND cells

| grid_id | centroid_lat | centroid_lon | hand_min | hand_mean | elevation_mean | slope_mean |
|---|---|---|---|---|---|---|
| KM_R007_C049 | 26.069 | 92.126 | 123.000 | 351.513 | 574.158 | 20.762 |
| KM_R007_C048 | 26.069 | 92.116 | 140.000 | 326.328 | 672.618 | 13.662 |
| KM_R008_C049 | 26.078 | 92.126 | 0.000 | 281.146 | 531.403 | 15.550 |
| KM_R006_C048 | 26.060 | 92.116 | 118.000 | 251.885 | 498.849 | 18.464 |
| KM_R008_C047 | 26.078 | 92.106 | 49.000 | 251.109 | 532.046 | 18.229 |
| KM_R009_C047 | 26.087 | 92.106 | 29.000 | 250.328 | 360.332 | 23.746 |
| KM_R018_C025 | 26.166 | 91.885 | 100.000 | 235.901 | 319.837 | 16.031 |
| KM_R021_C031 | 26.194 | 91.945 | 59.000 | 227.547 | 403.654 | 21.044 |
| KM_R008_C050 | 26.078 | 92.136 | 32.000 | 211.425 | 306.433 | 21.988 |
| KM_R008_C048 | 26.078 | 92.116 | 0.000 | 199.782 | 587.480 | 18.272 |

Map: `docs/img/flood_hand_map.png`. The reference point (26.14 N 91.77 E) is the approximate location given for the Bharalu corridor and was not independently surveyed here.

## 3. Sanity-check history: one check failed and was replaced

**The original check.** "The median `hand_mean` of hazard-site cells (cells floored to High because they contain a listed hazard site or a dated incident) is above the district median." It was meant to test that hill cells score high.

**What happened.** On the first run it failed: median `hand_mean` 7.02 m for 33 well-covered hazard-site cells, against a district median of 21.88 m. That run stopped before writing any output.

**Why the premise was wrong.** `app/susceptibility_utils.py` documents that documented landslide locations sit in cells *flatter* than the district average (median `slope_mean` 6.0 deg for the 6 incident cells and 4.5 deg for the 34 listed-hazard-site cells, against 9.4 deg for the district), which is the reason the hazard floor exists. This run agrees: hazard-site cells have median slope 4.1 deg against 9.5 deg, median elevation 60 m against 90 m, and 70% are flatter than the district median. A flat, low cell should get a low HAND, so the layer behaved correctly and the check was mis-specified: hazard sites are not hill cells.

**The replacement.** "The median `hand_mean` of the 71 steepest-decile cells (by `slope_mean`) is above the district median." Result: 115.2 m against 21.9 m, a pass.

**Two disclosures.** (1) The replacement was chosen *after* seeing the results. (2) It had already been computed and had already passed in a diagnostic run before it was adopted, so it was not a pre-registered test that could have failed. It is also a weak check on its own, because steep terrain has high HAND almost by construction (Spearman with slope is shown above). The informative checks are the lowest-10 and highest-10 lists, the corridor share, and the elevation correlation. None of the other six checks was changed.

## 4. Cross-check against MERIT Hydro channel distance

`data/raw/gee_flood_features.csv` has a valid `chan_dist_m` (MERIT Hydro, upa > 1 km2) for all 904 cells; every other flood column in that file was empty and is ignored. It also holds a `chan_dist_capped` column, which is not used. The GEE script that produced it was not available, so how MERIT's per-cell distance was defined (centroid or cell-wide statistic) is unverified.

| | Local (SRTM) | MERIT Hydro |
|---|---|---|
| Median (m) | 340 | 334 |
| Maximum (m) | 1867 | 1379 |

Over the 785 cells where both exist: **Pearson r = 0.794, Spearman = 0.780**; median absolute difference 69 m, mean signed difference (local minus MERIT) +7 m; 61% of cells agree within 100 m and 81% within 200 m.

**Agreement is moderate, and is NOT claimed as validation** (Spearman below the 0.85 bar set beforehand). The medians match, but individual cells can differ by hundreds of metres, and the local maximum is larger than MERIT's. The local values are written because they are the only complete set, not because MERIT confirmed them.

## 5. Sensitivity to the height surface

Using raw SRTM heights instead of depression-filled heights (negatives clipped to 0) changes per-cell `hand_mean` by a median of 0.94 m (Spearman between the two rankings 0.996, 802 cells).

## 6. Limitations

- **No urban drainage.** Storm drains, culverts, pipes, embankments, roads on fill and sluice gates are not in a DEM. Guwahati's urban flooding is largely a drainage-capacity problem that HAND cannot see.
- **30 m cannot resolve street-level drainage.** Roughly 1,170 pixels make up a 1 km cell; underpasses, kerbs and building-scale relief are averaged away. SRTM is a surface model: it includes tree canopy and buildings, and its integer-metre heights make flood-plain flats coarse.
- **Upstream area is truncated at the tile edge.** `upa_max` counts only what lies inside the DEM tile, so it understates the true catchment of any river that enters from outside, including the Brahmaputra.
- **D8 on a flat floodplain is arbitrary.** Where the surface is flat to within 1 m, flow paths are set by the flat-resolution step, not by real gradients, so channel positions there are approximate.
- **The DEM is whole metres.** SRTM heights are stored as integers, so flat ground within 1 m of a channel has HAND exactly 0: 17% of all DEM pixels (336,036) have HAND = 0, far more than the 36,723 channel pixels alone. HAND therefore takes coarse integer values, and `hand_min = 0` is common for any cell touching flat ground beside a channel.
- **Channels are a 1 km2 threshold, not a surveyed river network.** Small drains and ditches are not channels.
- **HAND is a static height, not water.** It does not model rainfall, discharge, backwater from the Brahmaputra, or duration. Low HAND means a cell sits near a drainage line, not that it floods.
- **Partly covered cells.** See section 1.

## 7. Implementation notes

- **numpy / pysheds shim.** `requirements.txt` pins numpy 2.5.2 and pysheds 0.5, but pysheds 0.5 calls `np.in1d`, which numpy 2.x removed, so `flow accumulation` crashes with an `AttributeError`. `app/flood_terrain.py` sets `np.in1d = np.isin` when it is missing (same behaviour for this use) before importing pysheds. Nothing in the environment or in `requirements.txt` was changed.
- **HAND is computed by a path walk written for this project,** not by pysheds' `compute_hand`, because pysheds leaves empty every pixel whose flow path touches the tile's edge ring. pysheds' result is kept as a cross-check (section above).
- **Use the pinned interpreter:** `venv\Scripts\python.exe app/flood_terrain.py`.
